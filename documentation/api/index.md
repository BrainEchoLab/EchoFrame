# API

The C++ core is generated from the source comments by Doxygen and rendered here
through Breathe. The MEX, Python and CLI pages also document their command and
call APIs, which live in dispatch code and argument parsing rather than in
documented symbols, so Doxygen cannot pick them up.

```{toctree}
:maxdepth: 2

efcore
beamformer
pdi
mex
python
cli
matlab
bindings
```
